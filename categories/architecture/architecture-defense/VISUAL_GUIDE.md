# Architecture Defense: Visual Guide

## The J1-J4 Framework at a Glance

```
┌─────────────────────────────────────────────────────────────────┐
│                    ARCHITECTURE DEFENSE SYSTEM                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ J1: SELF-DESCRIPTION                                            │
│ "Every module declares its own identity and boundaries"          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  packages/user-management/manifest.config.ts                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ id: '@my-org/user-management'                           │   │
│  │ purpose: 'User auth and profile management'              │   │
│  │                                                          │   │
│  │ publicAPI: {        ← What others CAN import            │   │
│  │   components: ['UserForm', 'UserProfile'],              │   │
│  │   services: ['UserService'],                            │   │
│  │   types: ['User', 'UserRole']                           │   │
│  │ }                                                        │   │
│  │                                                          │   │
│  │ internalAPI: {       ← What others CANNOT import        │   │
│  │   utils: ['passwordHash', 'validationUtils']            │   │
│  │ }                                                        │   │
│  │                                                          │   │
│  │ dependencies: {       ← What WE depend on               │   │
│  │   features: ['@my-org/auth'],                           │   │
│  │   ui: ['@my-org/design-system'],                        │   │
│  │   data: []           ← No direct database access!       │   │
│  │ }                                                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Output: Self-documenting architecture                          │
└─────────────────────────────────────────────────────────────────┘

                             ↓

┌─────────────────────────────────────────────────────────────────┐
│ J2: SELF-CHECK                                                   │
│ "The build validates itself against rules"                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  FRONTEND: dependency-cruiser                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ dependency-cruiser.config.cjs                           │   │
│  │                                                          │   │
│  │ forbidden: [                                            │   │
│  │   {                                                     │   │
│  │     name: 'no-internal-imports',                        │   │
│  │     from: 'src/features/[^/]+',                         │   │
│  │     to: 'src/features/[^/]+/internal/**',               │   │
│  │     allowSelf: true                                     │   │
│  │   },                                                    │   │
│  │   {                                                     │   │
│  │     name: 'no-data-layer-in-ui',                        │   │
│  │     from: 'src/**/components/**',                       │   │
│  │     to: ['src/data/**'],                                │   │
│  │     pathNot: ['**/*.types.ts']                          │   │
│  │   },                                                    │   │
│  │   {                                                     │   │
│  │     name: 'no-circular-dependencies',                   │   │
│  │     from: 'src',                                        │   │
│  │     to: { circular: true }                              │   │
│  │   }                                                     │   │
│  │ ]                                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  $ npm run architecture-check                                   │
│  ✖ no-internal-imports: OrderForm.tsx → user/internal/utils.ts │
│  ✖ no-data-layer-in-ui: Login.tsx → data/models/User.ts        │
│  ✖ no-circular-dependencies: user → auth → user                │
│                                                                  │
│  BACKEND: ArchUnit (Java/Kotlin)                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ @Test                                                   │   │
│  │ void uiLayerMustNotAccessDatabase() {                   │   │
│  │   noClasses()                                           │   │
│  │     .that().resideInAPackage("..ui..")                  │   │
│  │     .should().dependOnClassesThat()                     │   │
│  │     .resideInAPackage("..database..")                   │   │
│  │     .check(classes);  // Fails if violated!             │   │
│  │ }                                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Output: Automated validation                                    │
└─────────────────────────────────────────────────────────────────┘

                             ↓

┌─────────────────────────────────────────────────────────────────┐
│ J3: CLOSED RULES                                                 │
│ "Rules apply to the build itself"                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  package.json                                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ {                                                       │   │
│  │   "scripts": {                                         │   │
│  │     "type-check": "tsc --noEmit",                      │   │
│  │     "lint": "eslint src",                              │   │
│  │     "architecture-check": "depcruise src",             │   │
│  │     "prebuild": "npm run architecture-check",  ← J3!   │   │
│  │     "pretest": "npm run architecture-check",   ← J3!   │   │
│  │     "precommit": "npm run architecture-check" ← J3!   │   │
│  │   }                                                     │   │
│  │ }                                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Key insight: The build validates ITS OWN configuration!        │
│                                                                  │
│  dependency-cruiser.config.cjs validates:                       │
│  - No feature → internal imports                                │
│  - No UI → database imports                                     │
│  - No circular dependencies                                     │
│                                                                  │
│  These rules apply to dependency-cruiser.config.cjs ITSELF!     │
│                                                                  │
│  Output: Self-enforcing rules                                   │
└─────────────────────────────────────────────────────────────────┘

                             ↓

┌─────────────────────────────────────────────────────────────────┐
│ J4: REFLEX                                                       │
│ "Violations block CI/CD pipeline"                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  GitHub Actions workflow                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ .github/workflows/architecture.yml                      │   │
│  │                                                          │   │
│  │ on: pull_request                                        │   │
│  │                                                          │   │
│  │ jobs:                                                    │   │
│  │   architecture-check:                                   │   │
│  │     runs-on: ubuntu-latest                              │   │
│  │     steps:                                              │   │
│  │       - name: Run dependency-cruiser                    │   │
│  │         run: |                                          │   │
│  │           npx depcruise src > report.json               │   │
│  │           VIOLATIONS=$(jq '.summary.violations')        │   │
│  │           if [ "$VIOLATIONS" -gt 0 ]; then              │   │
│  │             exit 1  ← FAILS BUILD (J4!)                 │   │
│  │           fi                                            │   │
│  │                                                          │   │
│  │       - name: Comment PR                                 │   │
│  │         if: failure()                                   │   │
│  │         uses: actions/github-script@v7                  │   │
│  │         with:                                           │   │
│  │           script: |                                      │   │
│  │             github.rest.issues.createComment({          │   │
│  │               body: "🔴 Architecture violations found"   │   │
│  │             })                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  CODEOWNERS file                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ .github/CODEOWNERS                                      │   │
│  │                                                          │   │
│  │ # Architecture rules need expert approval               │   │
│  │ /dependency-cruiser.config.cjs  @architecture-team      │   │
│  │                                                          │   │
│  │ # Feature teams own their modules                       │   │
│  │ /src/features/user-management/** @user-team            │   │
│  │                                                          │   │
│  │ # Internal files need senior dev approval               │   │
│  │ **/internal/**  @senior-dev @tech-lead                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Result: PRs CANNOT merge if violations exist                   │
│                                                                  │
│  Output: Guaranteed compliance                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        COMPLETE FLOW                            │
└─────────────────────────────────────────────────────────────────┘

Developer creates PR
        ↓
   ┌─────────────────────┐
   │  J1: Manifest       │  ← Module declares public API
   └─────────────────────┘
        ↓
   ┌─────────────────────┐
   │  J2: Self-Check     │  ← dependency-cruiser runs
   └─────────────────────┘
        ↓
   ┌─────────────────────┐
   │  J3: Closed Rules   │  ← Build validates itself
   └─────────────────────┘
        ↓
   ┌─────────────────────┐
   │  J4: Reflex         │  ← CI blocks merge if violations
   └─────────────────────┘
        ↓
   Pass? ──YES──→ ✅ Merge allowed
        │
        NO
        ↓
   🔒 Blocked! Fix violations → Re-run check
```

## Example: Violation Detection Flow

```
┌──────────────────────────────────────────────────────────────────┐
│ SCENARIO: Developer violates architecture                       │
└──────────────────────────────────────────────────────────────────┘

Step 1: Developer writes bad code
──────────────────────────────────
File: src/features/orders/OrderForm.tsx
──────────────────────────────────────────────────────────────────
  import { UserUtils } from '@/features/user-management/internal/utils';
                                                                        ↑
                                                                        │
                                                        INTERNAL FILE!
                                                        Can't import!

Step 2: Developer commits and pushes
─────────────────────────────────────
  $ git add .
  $ git commit -m "feat: add user display to order form"
  $ git push origin feature/add-user-display
  $ gh pr create

Step 3: GitHub Actions runs automatically
─────────────────────────────────────────
  .github/workflows/architecture.yml triggered!

  ├─ Run dependency-cruiser
  │  $ npx depcruise --config dependency-cruiser.config.cjs src
  │
  │  ✖ no-internal-imports-from-other-features
  │    src/features/orders/OrderForm.tsx
  │      → src/features/user-management/internal/utils.ts
  │    ✖ Cannot import internal files from other features
  │
  │  Found 1 violation
  │  exit 1  ← BUILD FAILS

  └─ Comment on PR (because build failed)
     "🔴 Architecture violation detected
      OrderForm.tsx imports from user-management/internal/utils.ts
      Please fix before merging."

Step 4: Developer sees comment + failed check
──────────────────────────────────────────────
  PR Status: 🔴 All checks failed
  Comment from GitHub Actions:
  ┌──────────────────────────────────────────────────────────────┐
  │ ## 🔴 Architecture Violations Detected                      │
  │                                                              │
  │ Found 1 violation(s):                                        │
  │ - src/features/orders/OrderForm.tsx                         │
  │   → src/features/user-management/internal/utils.ts          │
  │   Cannot import internal files from other features          │
  │                                                              │
  │ Please fix before merging.                                   │
  └──────────────────────────────────────────────────────────────┘

  Merge button: DISABLED

Step 5: Developer fixes violation
─────────────────────────────────
Option 1: Use public API
  import { getUserDisplayName } from '@/features/user-management';

Option 2: Move utils to shared/
  import { UserUtils } from '@/shared/utils/user-utils';

Step 6: Developer pushes fix
────────────────────────────
  $ git add .
  $ git commit -m "fix: use public API instead of internal utils"
  $ git push

Step 7: CI runs again
────────────────────
  ├─ Run dependency-cruiser
  │  ✅ No violations found
  │
  └─ Comment on PR
     "✅ All architecture checks passed!"

  Merge button: ENABLED
```

## Architecture Rules Summary

```
┌─────────────────────────────────────────────────────────────────┐
│                    ARCHITECTURE RULES                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Rule 1: No Internal Imports Across Features                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ❌ BAD:                                                 │   │
│  │   OrderForm.tsx → user-management/internal/utils.ts     │   │
│  │                                                          │   │
│  │ ✅ GOOD:                                                │   │
│  │   OrderForm.tsx → user-management (public API)          │   │
│  │   OrderForm.tsx → shared/utils/user-utils.ts            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Rule 2: UI Cannot Access Database Layer                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ❌ BAD:                                                 │   │
│  │   Login.tsx → data/models/User.ts                       │   │
│  │                                                          │   │
│  │ ✅ GOOD:                                                │   │
│  │   Login.tsx → auth/service.ts (uses UserService)        │   │
│  │   UserService → data/models/User.ts (hidden from UI)    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Rule 3: No Circular Dependencies                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ❌ BAD:                                                 │   │
│  │   user → auth → notifications → user                   │   │
│  │                                                          │   │
│  │ ✅ GOOD:                                                │   │
│  │   user → shared-events                                  │   │
│  │   auth → shared-events                                  │   │
│  │   notifications → shared-events                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Rule 4: Enforce Layer Hierarchy                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │   ┌──────────┐                                           │   │
│  │   │   UI     │  Can only depend on: Services, Shared    │   │
│  │   └────┬─────┘                                           │   │
│  │        ↓                                                 │   │
│  │   ┌──────────┐                                           │   │
│  │   │ Services │  Can only depend on: Data, Shared        │   │
│  │   └────┬─────┘                                           │   │
│  │        ↓                                                 │   │
│  │   ┌──────────┐                                           │   │
│  │   │   Data   │  Can only depend on: Shared              │   │
│  │   └──────────┘                                           │   │
│  │                                                          │   │
│  │   Data CANNOT import from UI (inversion)                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Quick Reference

```
┌─────────────────────────────────────────────────────────────────┐
│                      QUICK REFERENCE                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Commands                                                        │
│  ─────────                                                       │
│  npm run architecture-check        Run validation               │
│  npm run architecture-check:html   View HTML report             │
│  npm run fix:interactive          Interactive fix mode          │
│                                                                  │
│  Files                                                           │
│  ─────                                                            │
│  manifest.config.ts                Module declaration (J1)      │
│  dependency-cruiser.config.cjs     Frontend rules (J2+J3)       │
│  ArchitectureRulesTest.java        Backend rules (J2+J3)        │
│  .github/workflows/architecture.yml CI enforcement (J4)         │
│  .github/CODEOWNERS                Team approval (J4)           │
│                                                                  │
│  J Principles                                                    │
│  ────────────                                                    │
│  J1  Self-Description   → manifest.config.ts                    │
│  J2  Self-Check         → dependency-cruiser + ArchUnit        │
│  J3  Closed Rules       → Embedded in build                    │
│  J4  Reflex             → CI blocks merge                       │
│                                                                  │
│  Common Violations                                                │
│  ────────────────                                                │
│  no-internal-imports         → Use public API or shared/       │
│  no-data-layer-in-ui         → Use service layer               │
│  no-circular-dependencies    → Refactor to shared/events       │
│                                                                  │
│  Next Steps                                                      │
│  ───────────                                                      │
│  1. Install: npm install -D dependency-cruiser                  │
│  2. Copy: *.example files to your repo                          │
│  3. Run: npm run architecture-check                             │
│  4. Fix: Iteratively fix violations                             │
│  5. Push: GitHub Actions will validate                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

**Created**: 2025-12-31
**Framework**: J1-J4 (Self-Governing Architecture)
**Tools**: dependency-cruiser, ArchUnit, GitHub Actions, CODEOWNERS
